apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-dlang-formatter-service
  namespace: uim-dlang-formatter
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-dlang-formatter-service
  template:
    metadata:
      labels:
        app: uim-dlang-formatter-service
    spec:
      containers:
      - name: dlang-formatter-service
        image: uim-dlang-formatter-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: dlang-formatter-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
