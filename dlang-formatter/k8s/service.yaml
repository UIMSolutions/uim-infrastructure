apiVersion: v1
kind: Service
metadata:
  name: uim-dlang-formatter-service
  namespace: uim-dlang-formatter
spec:
  selector:
    app: uim-dlang-formatter-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
