apiVersion: v1
kind: Service
metadata:
  name: uim-smtp-relay-service
  namespace: uim-smtp-relay
spec:
  type: ClusterIP
  selector:
    app: uim-smtp-relay-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
