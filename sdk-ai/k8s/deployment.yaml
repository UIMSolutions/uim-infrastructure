apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-sdk-ai-service
  namespace: uim-sdk-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-sdk-ai-service
  template:
    metadata:
      labels:
        app: uim-sdk-ai-service
    spec:
      containers:
      - name: sdk-ai
        image: uim-sdk-ai-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: sdk-ai-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
