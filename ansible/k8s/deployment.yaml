apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-ansible-service
  namespace: uim-ansible
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-ansible-service
  template:
    metadata:
      labels:
        app: uim-ansible-service
    spec:
      containers:
      - name: ansible
        image: uim-ansible-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: ansible-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
