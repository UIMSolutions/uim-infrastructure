apiVersion: v1
kind: Service
metadata:
  name: uim-ansible-service
  namespace: uim-ansible
spec:
  selector:
    app: uim-ansible-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
