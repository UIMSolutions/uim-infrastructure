apiVersion: v1
kind: Service
metadata:
  name: uim-dns-service
  namespace: uim-dns
spec:
  selector:
    app: uim-dns-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
