apiVersion: v1
kind: Service
metadata:
  name: uim-mcp-service
  namespace: uim-mcp
spec:
  selector:
    app: uim-mcp-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
