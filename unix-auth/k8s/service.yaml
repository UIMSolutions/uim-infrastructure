apiVersion: v1
kind: Service
metadata:
  name: uim-unix-auth-service
  namespace: uim-unix-auth
spec:
  selector:
    app: uim-unix-auth-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
