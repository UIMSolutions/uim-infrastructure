apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-ca-service
  namespace: uim-ca
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-ca-service
  template:
    metadata:
      labels:
        app: uim-ca-service
    spec:
      containers:
      - name: ca
        image: uim-ca-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9321
        envFrom:
        - configMapRef:
            name: ca-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 9321
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 9321
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
