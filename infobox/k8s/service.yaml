apiVersion: v1
kind: Service
metadata:
  name: uim-infobox-service
  namespace: uim-infobox
spec:
  selector:
    app: uim-infobox-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
