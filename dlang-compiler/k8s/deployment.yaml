apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-dlang-compiler-service
  namespace: uim-dlang-compiler
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-dlang-compiler-service
  template:
    metadata:
      labels:
        app: uim-dlang-compiler-service
    spec:
      containers:
      - name: dlang-compiler-service
        image: uim-dlang-compiler-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: dlang-compiler-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
