apiVersion: v1
kind: Service
metadata:
  name: uim-dlang-compiler-service
  namespace: uim-dlang-compiler
spec:
  selector:
    app: uim-dlang-compiler-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
