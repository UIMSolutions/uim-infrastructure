apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-kafka-service
  namespace: uim-kafka
  labels:
    app: uim-kafka-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-kafka-service
  template:
    metadata:
      labels:
        app: uim-kafka-service
    spec:
      containers:
        - name: uim-kafka-service
          image: uim-kafka-service:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: uim-kafka-config
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
