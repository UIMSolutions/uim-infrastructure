apiVersion: v1
kind: Service
metadata:
  name: uim-maia-service
  namespace: uim-maia
spec:
  selector:
    app: uim-maia-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
