apiVersion: v1
kind: Service
metadata:
  name: uim-logging-service
  namespace: uim-logging
spec:
  selector:
    app: uim-logging-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
