apiVersion: v1
kind: Service
metadata:
  name: uim-crossplane-service
  namespace: uim-crossplane
spec:
  selector:
    app: uim-crossplane-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
