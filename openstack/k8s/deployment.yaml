apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-openstack-service
  namespace: uim-openstack
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-openstack-service
  template:
    metadata:
      labels:
        app: uim-openstack-service
    spec:
      containers:
      - name: openstack
        image: uim-openstack-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: openstack-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
