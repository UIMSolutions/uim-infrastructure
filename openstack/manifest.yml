---
applications:
- name: uim-openstack-service
  memory: 256M
  instances: 1
  buildpacks:
  - binary_buildpack
  command: ./uim-openstack-service
  env:
    PORT: 8080
    BIND_ADDRESS: 0.0.0.0
    OPENSTACK_DEFAULT_REGION: RegionOne
