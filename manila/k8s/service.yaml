apiVersion: v1
kind: Service
metadata:
  name: uim-manila-service
  namespace: uim-manila
spec:
  selector:
    app: uim-manila-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
