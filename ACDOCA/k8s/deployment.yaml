apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-acdoca-service
  namespace: uim-acdoca
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-acdoca-service
  template:
    metadata:
      labels:
        app: uim-acdoca-service
    spec:
      containers:
      - name: acdoca-service
        image: uim-acdoca-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: acdoca-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
