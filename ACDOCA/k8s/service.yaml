apiVersion: v1
kind: Service
metadata:
  name: uim-acdoca-service
  namespace: uim-acdoca
spec:
  selector:
    app: uim-acdoca-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
