apiVersion: v1
kind: Service
metadata:
  name: uim-redis-service
  namespace: uim-redis
spec:
  selector:
    app: uim-redis-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
