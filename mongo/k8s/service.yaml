apiVersion: v1
kind: Service
metadata:
  name: uim-mongo-service
  namespace: uim-mongo
spec:
  selector:
    app: uim-mongo-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
