apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-scim-service
  namespace: uim-scim
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-scim-service
  template:
    metadata:
      labels:
        app: uim-scim-service
    spec:
      containers:
      - name: scim
        image: uim-scim-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: scim-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
