apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-vpc-service
  namespace: uim-vpc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-vpc-service
  template:
    metadata:
      labels:
        app: uim-vpc-service
    spec:
      containers:
      - name: vpc
        image: uim-vpc-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: vpc-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
