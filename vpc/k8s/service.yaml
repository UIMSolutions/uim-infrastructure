apiVersion: v1
kind: Service
metadata:
  name: uim-vpc-service
  namespace: uim-vpc
spec:
  selector:
    app: uim-vpc-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
