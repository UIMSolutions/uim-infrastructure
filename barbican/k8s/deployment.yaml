apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-barbican-service
  namespace: uim-barbican
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-barbican-service
  template:
    metadata:
      labels:
        app: uim-barbican-service
    spec:
      containers:
      - name: barbican
        image: uim-barbican-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9311
        envFrom:
        - configMapRef:
            name: barbican-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 9311
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 9311
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
