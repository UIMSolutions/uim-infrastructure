apiVersion: v1
kind: Service
metadata:
  name: uim-hts-service
  namespace: uim-hts
spec:
  selector:
    app: uim-hts-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
