apiVersion: v1
kind: Service
metadata:
  name: uim-vault-service
  namespace: uim-vault
spec:
  selector:
    app: uim-vault-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
