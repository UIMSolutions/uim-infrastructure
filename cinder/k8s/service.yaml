apiVersion: v1
kind: Service
metadata:
  name: uim-cinder-service
  namespace: uim-cinder
spec:
  selector:
    app: uim-cinder-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
