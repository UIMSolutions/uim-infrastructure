apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-cinder-service
  namespace: uim-cinder
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-cinder-service
  template:
    metadata:
      labels:
        app: uim-cinder-service
    spec:
      containers:
      - name: cinder
        image: uim-cinder-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: cinder-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
