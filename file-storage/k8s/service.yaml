apiVersion: v1
kind: Service
metadata:
  name: uim-file-storage-service
  namespace: uim-file-storage
spec:
  selector:
    app: uim-file-storage-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
