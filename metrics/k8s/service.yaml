apiVersion: v1
kind: Service
metadata:
  name: uim-metrics-service
  namespace: uim-metrics
spec:
  selector:
    app: uim-metrics-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
