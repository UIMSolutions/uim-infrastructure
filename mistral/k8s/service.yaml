apiVersion: v1
kind: Service
metadata:
  name: uim-mistral-service
  namespace: uim-mistral
spec:
  selector:
    app: uim-mistral-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
