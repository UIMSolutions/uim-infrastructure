apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-block-storage-service
  namespace: uim-block-storage
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-block-storage-service
  template:
    metadata:
      labels:
        app: uim-block-storage-service
    spec:
      containers:
      - name: block-storage
        image: uim-block-storage-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: block-storage-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
