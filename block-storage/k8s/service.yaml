apiVersion: v1
kind: Service
metadata:
  name: uim-block-storage-service
  namespace: uim-block-storage
spec:
  selector:
    app: uim-block-storage-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
