apiVersion: v1
kind: Service
metadata:
  name: uim-load-balancing-service
  namespace: uim-lb
spec:
  selector:
    app: uim-load-balancing-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
