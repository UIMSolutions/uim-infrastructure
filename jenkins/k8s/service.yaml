apiVersion: v1
kind: Service
metadata:
  name: uim-jenkins-service
  namespace: uim-jenkins
spec:
  selector:
    app: uim-jenkins-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
