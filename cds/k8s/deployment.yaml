apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-cds-service
  namespace: uim-cds
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-cds-service
  template:
    metadata:
      labels:
        app: uim-cds-service
    spec:
      containers:
      - name: cds-service
        image: uim-cds-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: cds-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
