apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-terraform-service
  namespace: uim-terraform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uim-terraform-service
  template:
    metadata:
      labels:
        app: uim-terraform-service
    spec:
      containers:
      - name: terraform
        image: uim-terraform-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: terraform-service-config
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
