apiVersion: v1
kind: Service
metadata:
  name: uim-waf-service
  namespace: uim-waf
spec:
  selector:
    app: uim-waf-service
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
